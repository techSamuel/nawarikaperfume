<IfModule mod_rewrite.c>
    RewriteEngine on
    # Route root request to public/index.php
    RewriteRule ^$ public/index.php [L]
    # Route any request that doesn't start with public/ to public/
    RewriteRule ^((?!public/).*)$ public/$1 [L,NC]
</IfModule>
